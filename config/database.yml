# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
# TODO: Setup actual db username and password below

development:
  adapter: mysql2
  encoding: utf8
  reconnect: false
  database: moviedb
  pool: 5
  #username: user
  #password: pass
  host: localhost

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: mysql2
  encoding: utf8
  reconnect: false
  database: moviedb_test
  pool: 5
  #username: user
  #password: pass
  host: localhost

production:
  adapter: mysql2
  encoding: utf8
  reconnect: false
  database: moviedb_prod
  pool: 5
  #username: user
  #password: pass
  host: localhost
